<Context>
	<Valve className="com.code.aon.jaas.vendor.tomcat.HttpServletRequestValve"/> 
	<Realm appName="aon.security" className="org.apache.catalina.realm.JAASRealm" 
		roleClassNames="com.code.aon.jaas.auth.SimpleGroup" 
		useContextClassLoader="false" 
		userClassNames="com.code.aon.jaas.auth.AuthPrincipal"/>
	<!--
	The following example shows a configuration for non-sticky sessions.
	In this case there's no need for failoverNodes, as sessions are served by
	all tomcats round-robin and they're not bound to a single tomcat.
	For non-sticky sessions the configuration (for both/all tomcats) would look like this
	-->
        <!--Manager
		createIfNotExist="true" 
		regionId="DYNAMODB_MANAGER_REGION_ID"
		className="com.amazonaws.services.dynamodb.sessionmanager.DynamoDBSessionManager" 
		>
        </Manager-->
   
</Context>
