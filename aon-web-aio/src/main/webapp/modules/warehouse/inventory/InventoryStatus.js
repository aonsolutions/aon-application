import { CONSTANT, MSG } from "../../../environments/environments";

export const inventoryStatuses = [
    {name: MSG.PROCESSED, value: CONSTANT.PROCESSED.toUpperCase()},
    {name: MSG.OPENED, value: CONSTANT.OPENED.toUpperCase()}
]