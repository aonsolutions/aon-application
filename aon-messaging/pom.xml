<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<parent>
		<groupId>com.code.aon</groupId>
		<artifactId>aon.parent</artifactId>
		<version>5.0.0</version>
	</parent>
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.code.aon</groupId>
	<artifactId>aon.messaging</artifactId>
	<version>${aon-version}</version>
	<description>AON MESSAGING</description>
	<dependencies>
		<dependency>
			<groupId>com.code.aon</groupId>
			<artifactId>aon.common</artifactId>
			<version>${aon-version}</version>
			<scope>compile</scope>
		</dependency>	
		<dependency>
			<groupId>com.esendex</groupId>
			<artifactId>Esendex</artifactId>
			<version>0.0.1</version>
			<scope>compile</scope>
		</dependency> 
		<dependency>
			<groupId>axis</groupId>
			<artifactId>axis</artifactId>
			<version>1.3</version>
			<scope>compile</scope>
			<exclusions>
				<exclusion>
					<groupId>commons-logging</groupId>
					<artifactId>commons-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency> 
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<artifactId>maven-jar-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
						 	<mainClass>com.code.aon.messaging.sms.Sender</mainClass>
							<!--  <packageName>com.mycompany.app</packageName> -->
							<!-- options
								<addClasspath>true</addClasspath>
								<addExtensions/>
								<classpathPrefix/>
							-->
						</manifest>
						<!-- 
						<manifestEntries>
							<mode>development</mode>
							<url>${pom.url}</url>
						</manifestEntries>
						<manifestFile>src/main/resources/META-INF/MANIFEST.MF</manifestFile>
						 -->
					</archive>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>