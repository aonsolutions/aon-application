package com.esferalia.aon.payroll.enumeration.ss;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Calendar;
import java.util.Date;
import com.esferalia.aon.payroll.enumeration.IPayrollTablesEnum;
import org.apache.commons.lang.time.DateUtils;

/** 
 * 
 * Autogenerated class.
 * 
 * ==================================
 * DO NOT CHANGE THIS!!!!!!!!!!!!!!
 * ==================================
 * Enumeration for represent SOCIAL SECURITY T41 table codes.
 * Generation main class: com.esferalia.aon.payroll.sepe.SSCodeTablesWriter
 *  ------------------------------------------------------------------------
 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
 * T41.txt
 *  ------------------------------------------------------------------------
 */ 
public enum T41 implements IPayrollTablesEnum {

	T41_1( "1", "Periodo de inactividad", null, null ),
	T41_2( "2", "Huelga total", null, null ),
	T41_3( "3", "Huelga parcial", null, null ),
	T41_4( "4", "Trienios de funcionarios", null, null ),
	T41_5( "5", "Ausencia del trabajo", null, null ),
	T41_6( "6", "Alta sin retribución", null, null ),
	T41_7( "7", "Funcionarios R. General. Permiso sin sueldo", null, null ),
	T41_8( "8", "Funcionarios R. General. Suspensión provisional", null, null ),
	T41_9( "9", "Funcionarios R. General. Suspensión provisional no firme", null, null ),
	T41_A( "A", "Funcionarios R. General. Suspensión provisional firme", null, null ),
	T41_E( "E", "Suspensión total ERE", null, null ),
	T41_F( "F", "Suspensión parcial ERE", null, null ),
	T41_G( "G", "Suspensión parcial ERE y Huelga parcial", null, null ),
	T41_H( "H", "Reservistas en periodos de activación", null, null ),
	T41_I( "I", "Suspensión total ERE fuerza mayor", null, null ),
	T41_J( "J", "Suspensión parcial ERE fuerza mayor", null, null ),
	T41_K( "K", "Suspensión parcial ERE fuerza mayor + huelga parcial", null, null ),
	T41_R( "R", "Reinicio de la actividad", null, null ),
	T41_S( "S", "Suspension parcial ERE Parcial COVID-19", null, null ),
	T41_T( "T", "Salarios de Tramitación", null, null ),
	T41_V( "V", "Suspension total ERE COVID-19", null, null ),
	T41_W( "W", "Suspension parcial ERE COVID-19", null, null ),
	;
	public static final String TABLE_NAME = "T41";
	public static final String TABLE_DESCRIPTION = "T41.txt";
	private final SimpleDateFormat sdf = new SimpleDateFormat("yyyyMMdd");
	private String code;
	private String description;
	private String startDate;
	private String endDate;

	T41( String code, String description, String startDate, String endDate ) {
		this.code = code;
		this.description = description;
		this.startDate = startDate;
		this.endDate = endDate;
	}

	public String getCode() {
		return code;
	}

	public String getDescription() {
		return description;
	}

	public Date getStartDate(){
		try {
			if(startDate!=null){
				return DateUtils.ceiling(sdf.parse(startDate), Calendar.DAY_OF_MONTH);
			}
		} catch (ParseException e) {
			// nothing to do
		}
		return null;
	}

	public Date getEndDate(){
		try {
			if(endDate!=null){
				return DateUtils.ceiling(sdf.parse(endDate), Calendar.DAY_OF_MONTH);
			}
		} catch (ParseException e) {
			// nothing to do
		}
		return null;
	}

	public boolean isActive(){
		Date now = new Date();
		now = DateUtils.ceiling(now, Calendar.DAY_OF_MONTH);
		if( (getStartDate()!=null && getStartDate().after(now)) || (getEndDate()!=null && getEndDate().before(now)) ){
			return false;
		}
		return true;
	}

	public static T41 getEnumByValue(String expression) {
		for( T41 o : T41.values() ) {
			if ( o.getCode().equals(expression) ) {
				return o;
			}
		}
		return null;
	}

}