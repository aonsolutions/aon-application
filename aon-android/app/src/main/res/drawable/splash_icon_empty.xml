<?xml version="1.0" encoding="utf-8"?>
<!-- Icono vacío para el splash del sistema: en Android 12+ ese icono siempre se
     recorta a un círculo, así que lo dejamos transparente y la imagen a pantalla
     completa la pinta la propia app. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/>
</shape>
