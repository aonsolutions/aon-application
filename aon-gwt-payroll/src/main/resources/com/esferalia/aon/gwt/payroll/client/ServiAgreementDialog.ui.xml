<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
	xmlns:g='urn:import:com.google.gwt.user.client.ui'
	xmlns:a='urn:import:com.esferalia.aon.gwt.payroll.client'
	xmlns:c='urn:import:com.esferalia.aon.gwt.common.client.widget'
	xmlns:d='urn:import:com.google.gwt.user.cellview.client' >

	<ui:with field='msgs' type='com.esferalia.aon.gwt.payroll.client.i18n.PayrollMessages'/>

	<ui:Style type="com.esferalia.aon.gwt.payroll.client.ServiAgreementDialog.MyStyle">
		
		.column{
			display: flex;
			flex-direction: column;
			gap: 1em;
			padding: 1em;
		}
		
		.flex{
			display: flex;
			gap: 1em;
		}
		
		.title{
			font-weight: bold;
			text-transform: uppercase;
			font-size: 1em;
			margin-right: 10px; 
		}
		
		.w400{
			width: 400px;
		}
		
		.message{
			color: #7d7d7d;
			max-width: 475px;
		}
		
		.datesPanel{
			display: flex;
			flex-direction: column;
			gap: .4em;
		}
		
		.mainButtonsPanel{
			display: flex;
			justify-content: space-between;
			align-items: center;
			width: 100%;
		}
		
		.buttonsPanel{
			display: flex;
			justify-content: flex-end;
			align-items: center;
			gap: 1em;
		}
		
		.loadingPanel{
			display: flex;
			align-items: center;
			animation-name: spin;
			animation-duration: 2500ms;
			animation-iteration-count: infinite;
			animation-timing-function: linear;
		}
		
		@keyframes spin {
		    from {
		        transform:rotate(0deg);
		    }
		    to {
		        transform:rotate(360deg);
		    }
		}
		
		.dialogGlass {
			z-index: 7;
			background-color: #000;
			opacity: 0.2;
			filter: opacity(45%);
		}
		
		.dialogZIndex {
			z-index: 8;
		}
		
	</ui:Style>

	<g:HTMLPanel styleName="{style.column}">
		<g:HTMLPanel styleName="{style.flex}">
			<g:Label styleName="{style.title}">{msgs.agreement}</g:Label>
			<g:SuggestBox ui:field="serviAgreementsSB" styleName="{style.w400}" />
		</g:HTMLPanel>
		<g:HTMLPanel ui:field="datesPanel" styleName="{style.datesPanel}" visible="false" />
		<g:HTMLPanel ui:field="messagePanel" styleName="{style.flex}">
			<g:Label styleName="{style.message}">{msgs.agreementImportInactive}</g:Label>
		</g:HTMLPanel>
		<g:HTMLPanel ui:field="mainButtonsPanel" styleName="{style.mainButtonsPanel}">
			<g:HTMLPanel ui:field="loadingPanel" styleName="{style.flex}" />
			<g:HTMLPanel ui:field="buttonsPanel" styleName="{style.buttonsPanel}" />
		</g:HTMLPanel>
	</g:HTMLPanel>

</ui:UiBinder>