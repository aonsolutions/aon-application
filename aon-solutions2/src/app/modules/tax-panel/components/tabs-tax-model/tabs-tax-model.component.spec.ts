/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TabsTaxModelComponent } from './tabs-tax-model.component';

describe('TabsTaxModelComponent', () => {
  let component: TabsTaxModelComponent;
  let fixture: ComponentFixture<TabsTaxModelComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TabsTaxModelComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TabsTaxModelComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

 // it('should create', () => {
 //   expect(component).toBeTruthy();
 // });
});
*/