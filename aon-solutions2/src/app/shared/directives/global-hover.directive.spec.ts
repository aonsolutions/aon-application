import { GlobalHoverDirective } from './global-hover.directive';

describe('GlobalHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new GlobalHoverDirective();
    expect(directive).toBeTruthy();
  });
});
