import { TediDropZoneDirective } from './drop-zone.directive';

describe('DropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TediDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
