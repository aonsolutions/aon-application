import { TediNoDropZoneDirective } from './no-drop-zone.directive';

describe('NoDropZoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TediNoDropZoneDirective();
    expect(directive).toBeTruthy();
  });
});
