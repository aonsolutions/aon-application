package com.esferalia.aon.ui.payroll.controller;

import java.io.Serializable;
import java.util.LinkedList;
import java.util.List;

import javax.faces.model.SelectItem;

import com.code.aon.common.AonVersion;
import com.esferalia.aon.payroll.enumeration.ss.*;

/** 
 * Autogenerated class
 * 
 * Contrat@ code tables Collections.
 * Generation main class: com.esferalia.aon.payroll.sepe.SSCodeTablesWriter
*/ 

public class SSCollectionsController implements Serializable {
	
	private static final long serialVersionUID = AonVersion.SERIAL_VERSION_UID;

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * SSCodeTables.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> SSCodeTablesCodeList;

	public List<SelectItem> getSSCodeTablesCodeList() {
		if (SSCodeTablesCodeList == null) {
			SSCodeTablesCodeList = new LinkedList<SelectItem>();
			SSCodeTables[] el = SSCodeTables.values();
			for (SSCodeTables obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					SSCodeTablesCodeList.add(item);
				}
			}
		}
		return SSCodeTablesCodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T01.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T01CodeList;

	public List<SelectItem> getT01CodeList() {
		if (T01CodeList == null) {
			T01CodeList = new LinkedList<SelectItem>();
			T01[] el = T01.values();
			for (T01 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T01CodeList.add(item);
				}
			}
		}
		return T01CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T05.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T05CodeList;

	public List<SelectItem> getT05CodeList() {
		if (T05CodeList == null) {
			T05CodeList = new LinkedList<SelectItem>();
			T05[] el = T05.values();
			for (T05 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T05CodeList.add(item);
				}
			}
		}
		return T05CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T06.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T06CodeList;

	public List<SelectItem> getT06CodeList() {
		if (T06CodeList == null) {
			T06CodeList = new LinkedList<SelectItem>();
			T06[] el = T06.values();
			for (T06 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T06CodeList.add(item);
				}
			}
		}
		return T06CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T07.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T07CodeList;

	public List<SelectItem> getT07CodeList() {
		if (T07CodeList == null) {
			T07CodeList = new LinkedList<SelectItem>();
			T07[] el = T07.values();
			for (T07 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T07CodeList.add(item);
				}
			}
		}
		return T07CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T10.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T10CodeList;

	public List<SelectItem> getT10CodeList() {
		if (T10CodeList == null) {
			T10CodeList = new LinkedList<SelectItem>();
			T10[] el = T10.values();
			for (T10 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T10CodeList.add(item);
				}
			}
		}
		return T10CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T18.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T18CodeList;

	public List<SelectItem> getT18CodeList() {
		if (T18CodeList == null) {
			T18CodeList = new LinkedList<SelectItem>();
			T18[] el = T18.values();
			for (T18 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T18CodeList.add(item);
				}
			}
		}
		return T18CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T21.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T21CodeList;

	public List<SelectItem> getT21CodeList() {
		if (T21CodeList == null) {
			T21CodeList = new LinkedList<SelectItem>();
			T21[] el = T21.values();
			for (T21 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T21CodeList.add(item);
				}
			}
		}
		return T21CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T37.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T37CodeList;

	public List<SelectItem> getT37CodeList() {
		if (T37CodeList == null) {
			T37CodeList = new LinkedList<SelectItem>();
			T37[] el = T37.values();
			for (T37 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T37CodeList.add(item);
				}
			}
		}
		return T37CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T41.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T41CodeList;

	public List<SelectItem> getT41CodeList() {
		if (T41CodeList == null) {
			T41CodeList = new LinkedList<SelectItem>();
			T41[] el = T41.values();
			for (T41 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T41CodeList.add(item);
				}
			}
		}
		return T41CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T54.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T54CodeList;

	public List<SelectItem> getT54CodeList() {
		if (T54CodeList == null) {
			T54CodeList = new LinkedList<SelectItem>();
			T54[] el = T54.values();
			for (T54 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T54CodeList.add(item);
				}
			}
		}
		return T54CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T58.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T58CodeList;

	public List<SelectItem> getT58CodeList() {
		if (T58CodeList == null) {
			T58CodeList = new LinkedList<SelectItem>();
			T58[] el = T58.values();
			for (T58 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T58CodeList.add(item);
				}
			}
		}
		return T58CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T61.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T61CodeList;

	public List<SelectItem> getT61CodeList() {
		if (T61CodeList == null) {
			T61CodeList = new LinkedList<SelectItem>();
			T61[] el = T61.values();
			for (T61 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T61CodeList.add(item);
				}
			}
		}
		return T61CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T68.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T68CodeList;

	public List<SelectItem> getT68CodeList() {
		if (T68CodeList == null) {
			T68CodeList = new LinkedList<SelectItem>();
			T68[] el = T68.values();
			for (T68 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T68CodeList.add(item);
				}
			}
		}
		return T68CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T83.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T83CodeList;

	public List<SelectItem> getT83CodeList() {
		if (T83CodeList == null) {
			T83CodeList = new LinkedList<SelectItem>();
			T83[] el = T83.values();
			for (T83 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T83CodeList.add(item);
				}
			}
		}
		return T83CodeList;
	}

	/** 
	 *  ------------------------------------------------------------------------
	 *  TABLA      	DESCRIPCION						FECHA ÚLTIMA ACTUALIZACIÓN.
	 * T84.java
	 *  ------------------------------------------------------------------------
	 */ 
	private List<SelectItem> T84CodeList;

	public List<SelectItem> getT84CodeList() {
		if (T84CodeList == null) {
			T84CodeList = new LinkedList<SelectItem>();
			T84[] el = T84.values();
			for (T84 obj : el) {
				if(obj.isActive()){
					String name = (obj.getDescription().length()>80?(obj.getDescription().substring(0, 80)+"..."):obj.getDescription());
					SelectItem item = new SelectItem(obj, name);
					T84CodeList.add(item);
				}
			}
		}
		return T84CodeList;
	}

}